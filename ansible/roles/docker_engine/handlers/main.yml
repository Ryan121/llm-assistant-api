---
- name: Warn about group re-login
  ansible.builtin.debug:
    msg: >-
      {{ ansible_user_id }} was added to the docker group. Log out and back in
      (or run `newgrp docker`) before `make up`, otherwise Docker will refuse
      with a permission error on the socket.
