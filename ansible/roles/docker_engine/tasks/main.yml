---
- name: Check whether Docker is already installed
  ansible.builtin.command: docker --version
  register: docker_engine_check
  changed_when: false
  failed_when: false

- name: Record the Docker version
  ansible.builtin.set_fact:
    docker_engine_version: "{{ docker_engine_check.stdout | default('absent') }}"

- name: Install Docker Engine from the official repository
  when: docker_engine_check.rc != 0
  become: true
  block:
    - name: Fail on unsupported distributions
      ansible.builtin.fail:
        msg: >-
          Automatic Docker installation is only wired up for Debian/Ubuntu
          (found {{ ansible_distribution }}). Install Docker Engine and the
          Compose v2 plugin yourself, then re-run with
          -e manage_docker_engine=false.
      when: ansible_os_family != 'Debian'

    - name: Install repository prerequisites
      ansible.builtin.apt:
        name:
          - ca-certificates
          - curl
          - gnupg
        state: present
        update_cache: true
        cache_valid_time: 3600

    - name: Create the keyring directory
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        mode: "0755"

    - name: Fetch the Docker GPG key
      ansible.builtin.get_url:
        url: "https://download.docker.com/linux/{{ ansible_distribution | lower }}/gpg"
        dest: /etc/apt/keyrings/docker.asc
        mode: "0644"
        force: false

    - name: Add the Docker apt repository
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch={{ 'amd64' if ansible_architecture == 'x86_64' else 'arm64' }}
          signed-by=/etc/apt/keyrings/docker.asc]
          https://download.docker.com/linux/{{ ansible_distribution | lower }}
          {{ ansible_distribution_release }} stable
        filename: docker
        state: present

    - name: Install Docker Engine and the Compose plugin
      ansible.builtin.apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: true

    - name: Enable and start Docker
      ansible.builtin.systemd:
        name: docker
        enabled: true
        state: started

    - name: Re-read the installed version
      ansible.builtin.command: docker --version
      register: docker_engine_check_post
      changed_when: false

    - name: Update the recorded version
      ansible.builtin.set_fact:
        docker_engine_version: "{{ docker_engine_check_post.stdout }}"

- name: Verify Compose v2 is available
  ansible.builtin.command: docker compose version
  register: docker_engine_compose_check
  changed_when: false
  failed_when: false

- name: Require the Compose v2 plugin
  ansible.builtin.assert:
    that:
      - docker_engine_compose_check.rc == 0
    fail_msg: >-
      `docker compose` is unavailable. Install the docker-compose-plugin
      package; the legacy standalone docker-compose v1 is not supported
      (this stack uses `profiles`, `depends_on.condition` and merge anchors).
    success_msg: "{{ docker_engine_compose_check.stdout | default('') }}"

- name: Add the deploying user to the docker group
  become: true
  ansible.builtin.user:
    name: "{{ ansible_user_id }}"
    groups: docker
    append: true
  when: ansible_user_id != 'root'
  notify: Warn about group re-login

# Everything after provisioning -- terraform, docker compose, make up -- runs
# as this user against the socket directly, so it is worth failing here with a
# usable message rather than three steps later with a raw EACCES.
- name: Check whether the deploying user can reach the Docker socket now
  ansible.builtin.command: docker info --format '{% raw %}{{.ServerVersion}}{% endraw %}'
  register: docker_engine_socket_check
  changed_when: false
  failed_when: false
  become: false

- name: Require socket access, or explain how to get it
  ansible.builtin.assert:
    that:
      - docker_engine_socket_check.rc == 0
    fail_msg: |-
      {{ ansible_user_id }} is in the docker group in /etc/group, but this
      login session was created before that and still carries the old
      supplementary groups. Nothing else can talk to Docker until that is
      refreshed. Pick one:

        newgrp docker && make quickstart     # new group in this shell
        sg docker -c 'make quickstart'       # one-off, no re-login
        <log out and back in>                # or reconnect the SSH session

      Provisioning itself is done -- re-running it after re-login is a no-op.
    success_msg: "docker socket reachable as {{ ansible_user_id }} (server {{ docker_engine_socket_check.stdout | trim }})"
  when: ansible_user_id != 'root'
