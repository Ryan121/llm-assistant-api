---
- name: Reload sysctl
  become: true
  ansible.builtin.command: sysctl --system
  changed_when: true

- name: Reload systemd and restart docker
  become: true
  ansible.builtin.systemd:
    name: docker
    daemon_reload: true
    state: restarted
